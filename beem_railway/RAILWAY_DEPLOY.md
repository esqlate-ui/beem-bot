# 🚂 Деплой на Railway — Пошаговая инструкция

## Шаг 1 — Загрузи файлы на GitHub

1. Зайди на https://github.com → нажми **New repository**
2. Назови репозиторий `beem-bot`, выбери **Private**, нажми **Create**
3. На странице репозитория нажми **uploading an existing file**
4. Перетащи ВСЕ файлы из этой папки → нажми **Commit changes**

## Шаг 2 — Создай проект на Railway

1. Зайди на https://railway.app → **Login with GitHub**
2. Нажми **New Project → Deploy from GitHub repo**
3. Выбери репозиторий `beem-bot` → нажми **Deploy Now**

## Шаг 3 — Добавь PostgreSQL базу данных

1. В проекте нажми **+ New** → **Database** → **Add PostgreSQL**
2. Railway автоматически создаст переменную `DATABASE_URL` ✅

## Шаг 4 — Добавь переменные окружения

1. Нажми на сервис бота (не базу данных)
2. Перейди во вкладку **Variables**
3. Добавь эти переменные:

| Имя переменной | Значение |
|---|---|
| `BOT_TOKEN` | Токен от @BotFather |
| `ADMIN_IDS` | Твой Telegram ID (число) |
| `ADMIN_PASSWORD` | Пароль для веб-панели |
| `ADMIN_SECRET` | Любая случайная строка |

## Шаг 5 — Готово!

Railway автоматически перезапустит бота с новыми переменными.
В логах должно появиться: `🐝 Beem Bot запущен!`

## Шаг 6 — Открыть веб-панель

1. Перейди во вкладку **Settings** → **Networking** → нажми **Generate Domain**
2. Railway даст тебе URL вида `beem-bot.up.railway.app`
3. Открой этот URL в браузере и войди с паролем из `ADMIN_PASSWORD`

---
💡 **Бесплатный лимит:** $5 кредитов/месяц. Для небольшого бота хватает надолго.
